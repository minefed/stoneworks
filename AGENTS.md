# Minefed 모드 저장소 작업 지침

이 지침은 이 저장소 전체와 모든 하위 모드/서브모듈 작업에 적용한다.
하위 저장소에서 작업을 시작할 때도 이 파일과 해당 저장소의 AGENTS.md를 함께 읽는다.

- 변경은 한 가지 목적을 가진 작고 검토 가능하며 되돌릴 수 있는 단위로 나눈다.
- 각 단위의 확인을 마친 즉시 커밋한다. 서로 무관한 수정이나 여러 모드의 기능 변경을 한 커밋에 섞지 않는다.
- 모든 새 커밋 메시지는 Conventional Commits를 따른다: `type(scope): description`.
  예: `docs(agents): define contribution rules`, `chore(mtr): update source pin`, `fix(build): include runtime jars`.
- 서브모듈 코드를 수정하면 해당 저장소에서 먼저 커밋한 뒤 상위 저장소에서 gitlink 변경을 별도 커밋한다.
- gitlink는 다른 작업자가 가져올 수 있는 원격 커밋을 가리켜야 한다. 로컬에만 존재하는 하위 커밋을 최종 gitlink로 남기지 않는다.
- 기존 변경을 덮어쓰거나 이력을 재작성하지 않는다. 모드 업데이트에는 추적 브랜치, 전체 커밋 해시, Minecraft/로더 호환성을 기록한다.
- 운영 모드 목록, JAR 해시와 출처, 소스 및 바이너리 라이선스를 함께 관리한다. 소스 공개 여부만으로 재배포 허용을 추정하지 않는다.
- 모드별 라이선스와 저작권 고지를 보존한다. 공개 재배포 권한을 확인하지 못한 JAR는 Git에 추가하지 않고 로컬 전용으로 관리한다.
- 운영 서버의 파일 변경, 업로드, 재시작은 별도 요청이 있을 때만 수행한다.
- 새 모드/서브모듈을 등록할 때도 이 지침의 적용 범위를 유지하고, 독립적인 하위 작업에서는 상위 지침을 명시적으로 전달한다.
